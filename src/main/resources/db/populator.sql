# CREATE TABLE IF NOT EXISTS core_test.role
# (
#     id          SERIAL PRIMARY KEY,
#     name        VARCHAR(255) NOT NULL UNIQUE,
#     description VARCHAR(255) NOT NULL
# );
#
# CREATE TABLE IF NOT EXISTS